
for (let multiplicador = 1; multiplicador < 10; multiplicador++) {
    for (let index = 0; index < 10; index++) {
        let calc = multiplicador * index;
        console.log(`${multiplicador} x ${index} = ${calc}`);
    }
    console.log("----------");
}